BEGIN
  EXECUTE IMMEDIATE 'DROP TABLE dts_by_month PURGE';
EXCEPTION  
  WHEN OTHERS THEN 
    NULL;
END;
/
CREATE TABLE DTS_BY_MONTH
(
  YR       NUMBER,
  MO       NUMBER,
  DAY      NUMBER,
  NEWDATE  DATE
)
/
